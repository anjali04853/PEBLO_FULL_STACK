# Base URL of the backend API.
# Leave as-is for local dev (Vite proxies /api -> localhost:5000).
# In production set this to your deployed backend, e.g. https://api.example.com/api
VITE_API_URL=/api
